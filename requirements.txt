# Mosquitoes — RF-DETR Python dependencies
# Python >= 3.10 required.
#
# Install PyTorch for your platform first if the lines below are not enough:
#   https://pytorch.org/get-started/locally/
#
#   pip install -r requirements.txt

# --- Dataset conversion ---
kagglehub>=1.0.0
pandas>=2.0
Pillow>=10.0

# --- RF-DETR pipeline ---
numpy>=1.24
supervision>=0.27
rfdetr>=1.6
torch>=2.0
torchvision>=0.15
